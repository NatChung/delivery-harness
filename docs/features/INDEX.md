# Feature 索引(registry)

> `/feature` pipeline 的 feature 清單 + id 配號來源(取最大 id +1)。
> 每個 feature 一個資料夾 `docs/features/<NNN>-<slug>/`。新 feature 由 `scripts/feature/cli.py new` 建立。

| id | slug | track | phase | created |
|----|------|-------|-------|---------|
| 001 | smoke-probe | lite | 0-intake | 2026-06-24 |
| 002 | smoke-probe | lite | 0-intake | 2026-06-24 |
